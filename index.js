// Code your solutions in this fil

const name = [ 'Lisa', 'Kaitlin', 'Jan' ]
const writeCards = (name) => {
  for (let i = 0; i < name.length; i++)
 return `Thank you, ${name[i]}, for the wonderful surprise gift!`
}